[project]
name = "jpvocab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "imnotkeril", email = "champ59ru@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "beartype>=0.22.9",
    "genanki>=0.13.1",
    "jamdict>=0.1a11.post2",
    "jamdict-data>=1.5",
    "requests>=2.34.2",
    "zstandard>=0.25.0",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
